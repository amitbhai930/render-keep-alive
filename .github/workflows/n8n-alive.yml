name: Render Keep Alive (No Code Change)

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          curl -L -s https://https://n8n-pr4a.onrender.com > /dev/null
